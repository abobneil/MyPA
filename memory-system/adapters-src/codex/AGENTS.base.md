# Project Memory

Use the compiled memory below as durable context for this repository. If a fact
looks unstable or task-specific, capture it in the inbox or a session note
instead of rewriting stable memory.
